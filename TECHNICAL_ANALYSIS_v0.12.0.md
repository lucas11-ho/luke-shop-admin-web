# Technical analysis — Merchant Admin v0.12.0

The Store Settings customer-service section now owns only public chat-routing metadata: Luke CS HTTPS Chat URL, platform route key, placement and label. Long-lived Shop credentials continue to live in the protected Customer Service integration area and are not serialized into storefront settings. The UI explains the v2 signed-context/server-token boundary and read-only policy.
