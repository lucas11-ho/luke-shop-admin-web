# Technical Analysis v0.4.0

The client admin remains tenant-scoped. Customer-facing changes are stored as versioned backend configuration rather than arbitrary HTML/JavaScript. Customer Web/Mobile consume only published configuration. Draft and rollback operations are audited by Backend v0.7.0.
