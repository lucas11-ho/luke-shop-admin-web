import fs from 'node:fs';const files=['src/styles.css','src/components/AppShell.jsx','src/pages/LoginPage.jsx','src/pages/CustomerExperiencePage.jsx'];let all='';for(const f of files){if(fs.existsSync(f))all+=fs.readFileSync(f,'utf8')+'\n'}let passed=0;const check=(v,m)=>{if(!all.includes(v))throw new Error(`Missing ${m}`);console.log(`PASS ${m}`);passed++};check('professional-admin','professional merchant shell');check('nav-group-label','grouped commerce navigation');check('experience-layout','Customer Experience workspace');check('feature-grid','Customer Experience feature controls');check('refined-builder','refined home builder');check('builder-fields','campaign media and CTA fields');check('calendar-popover','popup promotion calendar');check('font-preset-grid','typography preset chooser');check('template-library','template library');console.log(`${passed}/${passed} Admin Web v0.7.0 design checks passed`);
