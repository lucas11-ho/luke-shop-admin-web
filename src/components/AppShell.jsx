import React,{useEffect,useState}from'react';import{useAuth}from'../auth/AuthContext.jsx';import{navigate}from'../app/router.js';
const groups=[
 ['Operate',[['dashboard','Dashboard',null,'⌂'],['orders','Orders','orders.read','▤'],['products','Products','catalog.read','□'],['media-library','Media Library','catalog.read','▧'],['inventory','Inventory','inventory.read','≋'],['customers','Customers','customers.read','◎']]],
 ['Grow',[['promotions','Promotions','promotions.read','◇'],['payments','Payments','payments.read','$'],['delivery','Delivery','delivery.read','→']]],
 ['Experience',[['customer-experience','Customer Experience','customer_experience.read','✦'],['cs-ai','Luke CS & AI','integrations.customer_service.read','◉']]],
 ['System',[['settings','Store settings','tenant.settings.read','⚙'],['access','Access & roles',null,'⌘']]],
];
export function AppShell({route,children}){const{session,logout,updateStore}=useAuth();const[mobile,setMobile]=useState(false);const[store,setStore]=useState(session?.storeId||'');useEffect(()=>setMobile(false),[route]);const go=key=>navigate(`/${key}`);const title=groups.flatMap(x=>x[1]).find(([key])=>route===`/${key}`)?.[1]||'Workspace';return <div className="app-shell professional-admin"><aside className={`sidebar ${mobile?'open':''}`}><div className="brand"><div className="brand-mark">L</div><div><strong>Luke Shop</strong><span>Merchant workspace</span></div></div><div className="tenant-chip"><span>Tenant</span><strong>{session?.tenantSlug}</strong></div><nav>{groups.map(([group,items])=><div className="nav-group" key={group}><div className="nav-group-label">{group}</div>{items.map(([key,label,perm,icon])=>{if(perm&&!session?.user?.permissions?.includes(perm))return null;return <button key={key} className={route===`/${key}`?'active':''} onClick={()=>go(key)}><span className="nav-icon">{icon}</span><span>{label}</span></button>})}</div>)}</nav><div className="sidebar-foot"><div className="user-mini"><div className="avatar">{(session?.user?.display_name||session?.user?.email||'U').slice(0,1).toUpperCase()}</div><div><strong>{session?.user?.display_name||'Merchant'}</strong><span>{session?.user?.role||'Merchant user'}</span></div></div><button className="ghost full" onClick={logout}>Sign out</button></div></aside><div className="workspace"><header className="topbar"><button className="mobile-menu" onClick={()=>setMobile(!mobile)}>☰</button><div className="crumb"><span>Workspace</span><strong>{title}</strong></div><div className="topbar-actions"><label className="store-context"><span>Store context</span><input value={store} onChange={e=>setStore(e.target.value)} onBlur={()=>updateStore(store)} placeholder="Primary store" title="Leave empty to use primary store"/></label><div className="env-pill"><i/>Live API</div></div></header><main>{children}</main></div></div>}
