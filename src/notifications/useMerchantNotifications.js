import{useCallback,useEffect,useRef,useState}from'react';import{useAuth}from'../auth/AuthContext.jsx';
const SOUND_KEY='luke-shop-admin.order-sound.v1';
export function useMerchantNotifications(){const{api,session}=useAuth();const[items,setItems]=useState([]),[unread,setUnread]=useState(0),[newOrders,setNewOrders]=useState(0),[soundEnabled,setSoundEnabledState]=useState(()=>localStorage.getItem(SOUND_KEY)!=='off'),[loading,setLoading]=useState(false);const seen=useRef(new Set()),first=useRef(true),audio=useRef(null),mounted=useRef(true);const canRead=session?.user?.permissions?.includes('orders.read');
 useEffect(()=>{mounted.current=true;const unlock=()=>{if(!audio.current){try{audio.current=new(window.AudioContext||window.webkitAudioContext)()}catch{}}audio.current?.resume?.().catch(()=>{});};window.addEventListener('pointerdown',unlock,{passive:true});return()=>{mounted.current=false;window.removeEventListener('pointerdown',unlock)}},[]);
 const chime=()=>{if(!soundEnabled||!audio.current||audio.current.state!=='running')return;const ctx=audio.current,now=ctx.currentTime;[[659.25,0,.12],[783.99,.14,.16]].forEach(([freq,delay,dur])=>{const o=ctx.createOscillator(),g=ctx.createGain();o.type='sine';o.frequency.value=freq;g.gain.setValueAtTime(.0001,now+delay);g.gain.exponentialRampToValueAtTime(.12,now+delay+.01);g.gain.exponentialRampToValueAtTime(.0001,now+delay+dur);o.connect(g);g.connect(ctx.destination);o.start(now+delay);o.stop(now+delay+dur+.02)});};
 const refresh=useCallback(async({silent=false}={})=>{if(!canRead)return;if(!silent)setLoading(true);try{const d=await api.request('/v1/merchant/notifications',{query:{limit:40}});if(!mounted.current)return;const next=d.data.notifications||[];if(first.current){next.forEach(n=>seen.current.add(n.id));first.current=false}else{const fresh=next.filter(n=>!seen.current.has(n.id));fresh.forEach(n=>seen.current.add(n.id));if(fresh.some(n=>n.notification_type==='ORDER_CREATED'&&!n.read_at))chime();}setItems(next);setUnread(d.data.unread||0);setNewOrders(d.data.new_orders||0);}catch{}finally{if(mounted.current)setLoading(false)}},[api,canRead,soundEnabled,session?.storeId]);
 useEffect(()=>{first.current=true;seen.current=new Set();refresh();if(!canRead)return;const id=setInterval(()=>refresh({silent:true}),6000);return()=>clearInterval(id)},[refresh,canRead,session?.storeId]);
 const markRead=async id=>{await api.request(`/v1/merchant/notifications/${encodeURIComponent(id)}/read`,{method:'POST',body:{}});await refresh({silent:true})};const markAll=async()=>{await api.request('/v1/merchant/notifications/read-all',{method:'POST',body:{}});await refresh({silent:true})};const setSoundEnabled=value=>{setSoundEnabledState(value);localStorage.setItem(SOUND_KEY,value?'on':'off');if(value){try{if(!audio.current)audio.current=new(window.AudioContext||window.webkitAudioContext)();audio.current.resume?.()}catch{}}};const testSound=()=>chime();return{items,unread,newOrders,loading,refresh,markRead,markAll,soundEnabled,setSoundEnabled,testSound};}
